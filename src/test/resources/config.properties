# =================================================================
# ?? APPLICATION UNDER TEST (AUT) CONFIGURATION
# =================================================================
# Root URL for UI Automated Execution Cycles
ui.base.url=https://practice.expandtesting.com/notes/app/login

# Base Endpoint for Backend RESTful Services Testing
api.base.url=https://practice.expandtesting.com/notes/api

# =================================================================
# ? DEFAULT TARGET USER CREDENTIALS
# =================================================================
# Account credentials mapped directly from your Master Test Lists
default.username=rongheajinkya72@gmail.com
default.password=ajinkya72

# =================================================================
# ?? ENGINE TIMEOUT MANAGEMENT STRATEGIES
# =================================================================
# Maximum wait duration for conditional explicit state verifications (Seconds)
timeout.explicit=10

# Baseline cushion wait duration for general interactions (Seconds)
timeout.implicit=5

# RestAssured maximum acceptable backend latency benchmark (Milliseconds)
api.max.response.time.ms=2000

# =================================================================
# ? LOCAL FILE SYSTEM INGESTION & REPORTING PATHS
# =================================================================
# Relative project location pointer targeting the Master Excel Data Workbook
excel.testdata.path=./src/test/resources/TestData.xlsx

# Directory assignment for target execution output artifacts
allure.results.directory=./allure-results
screenshot.output.directory=./target/screenshots